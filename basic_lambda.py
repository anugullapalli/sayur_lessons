# Lambda function to add two numbers
add = lambda x, y, z: x + y + z
subtract = lambda x, y: x - y
multiply = lambda x, y: x * y
divide = lambda x, y: x / y
# Example usage
print(add(5, 3, 4))  # Output: 8
print(subtract(8,3))
print(multiply(2,3))
print(divide(8,2))
